# 港股十档盘口快照当前覆盖状态：2026-05-25

记录日期：2026-05-25（Asia/Shanghai）。

## 当前覆盖结论

本记录汇总 `2026-05-24` 下载补齐结果和 `2026-05-25` 冷归档验收结果。覆盖表述限定为
provider 历史窗口内当前账号可访问的港股 `CS` 标的。

| 项目 | 状态 |
| --- | --- |
| 最新已确认可取交易日 | `2026-05-22` |
| 全窗口 HK CS 并集 | `2,810` 只 |
| 应有上市日期单元 | `753,751` 个 |
| 本地缺失单元 | `0` |
| ETF | tick 接口权限拒绝，未纳入覆盖集合 |
| Historical delisted zero36 | provider 全空返回，保留 health 失败记录，不生成日频数据 |

以上覆盖范围包含当前账号权限内的 HK CS 历史十档盘口快照。范围外数据包括 ETF 权限外
快照、provider 全空返回数据集以及账号不可访问的数据。

## 冷归档验收

全量季度 compact 输出于 `2026-05-25` 完成 health 验收：

| 项目 | 结果 |
| --- | --- |
| 去重后输入字节数 | `39,760,427,880` |
| compact 输出字节数 | `15,898,022,941` |
| 字节减少比例 | `60.02%` |
| health 行数 / failure | `445,397,450` / `0` |
| tar 分包 | `9` 个，SHA-256 复核 `9 / 9` 通过 |

细节、命令与输出路径见
[2026-05-24 download progress](2026-05-24-hk-tick-download-progress.md) 和
[2026-05-24 compact benchmark](2026-05-24-hk-tick-compact-benchmark.md)。

## 后续增量维护

历史窗口补齐阶段已完成。后续日常操作围绕新增可取交易日：

1. 检查 quota，并对参考标的确认最新可取交易日。
2. 对新增交易日运行 `rqdata-hk-depth download --resume`。
3. 对新增原始快照运行 `health`，随后更新日频聚合和必要对账。
4. 覆盖范围、provider 权限或异常结果发生变化时新增 dated record。
5. 常规归档使用当前摘要；完整逐日流水只在审计或复核需要时显式打包。

历史数据目录、schema ID 和旧 CLI 入口继续作为兼容标识使用。
